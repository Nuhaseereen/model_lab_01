#include <stdio.h>
#define pi 3.14
int main()
{
  float radius;
  printf("enter the radius of circle\n");
  scanf("%f",&radius);
  printf("the area of circle is %.2f",radius*radius*pi);
    return 0;
}
